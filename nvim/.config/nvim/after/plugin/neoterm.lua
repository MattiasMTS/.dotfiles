vim.g.neoterm_command_prefix = "" -- to clear and go into insert mode. Default ""
vim.g.neoterm_repl_python = { "ipython" } -- no need to have poetry shell here.
vim.g.neoterm_same_shell = 1 -- same shell window (default=1)
vim.g.neoterm_autoscroll = 0 -- go to the bottom of the shell every time (default=0)
vim.g.neoterm_auto_repl_cmd = 1 -- go to new shell window. (default=1)
vim.g.neoterm_use_relative_path = 1 -- use relative linepath instead of absolute (default=0).
-- vim.g.neoterm_callbacks # TODO use to try tests using python instead of ipython
